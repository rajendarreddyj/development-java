language: java
sudo: false
jdk:
  - oraclejdk8
script:
  - ./mvnw clean install -Dmaven.test.failure.ignore=true
  - cd spring-gradle-projects && ./gradlew build
after_success:
  - ./mvnw clean test -Dmaven.test.failure.ignore=true jacoco:report coveralls:report
